services:
  website:
    build: .
    container_name: el-alem-portfolio
    ports:
      - "3004:80"
    restart: unless-stopped
    # Optional: Limit resources for NAS
    deploy:
      resources:
        limits:
          cpus: '0.50'
          memory: 256M
